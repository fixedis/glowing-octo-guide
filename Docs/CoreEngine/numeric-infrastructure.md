<!-- Docs/CoreEngine/numeric-infrastructure.md -->

# Числовая инфраструктура CoreEngine

CoreEngine предоставляет только низкоуровневую числовую инфраструктуру.

Он не содержит:

- ресурсов;
- характеристик;
- бонусов;
- штрафов;
- экономики;
- боя;
- конкретных игровых формул.

---

## MathKernel

`MathKernel` отвечает за безопасные числовые операции.

Основные возможности:

```text
add
subtract
multiply
multiplyByFactor
divide
clamp
fromFloat
toFloat
```

---

## Fixed point

По умолчанию используется масштаб:

```text
1_000_000
```

Это означает:

```text
1.0 = 1_000_000
0.5 = 500_000
1.25 = 1_250_000
```

Масштаб можно менять в методах, если это необходимо.

---

## RoundingMode

Поддерживаются режимы округления:

```text
HalfUp
HalfDown
Floor
Ceiling
TowardsZero
```

---

## Правила числовой логики

1. Критичная симуляция не должна использовать float.
2. Float допустим только на границах ввода/вывода или для отображения.
3. Все расчёты должны быть детерминированными.
4. Все деления должны использовать контролируемый масштаб.
5. Все округления должны быть явными.
6. Все ограничения должны быть явными.
7. Все доменные формулы принадлежат движкам, а не CoreEngine.

---

## DeterministicRandom

`DeterministicRandom` предоставляет повторяемый RNG.

Параметры:

```text
seed
tick
scope
counter
```

Генерация основана на SHA-256.

---

## Методы DeterministicRandom

```text
setTick
nextInt
nextFixed
chance
```

---

## nextInt

Возвращает целое число в диапазоне:

```text
min..max
```

---

## nextFixed

Возвращает значение в диапазоне:

```text
0..scale
```

---

## chance

Проверяет вероятность.

`probability` задаётся в том же масштабе, что и `scale`.

Пример:

```text
scale = 1_000_000
probability = 500_000
```

означает вероятность 50%.

---

## Правила использования RNG

1. Запрещено использовать `rand()`.
2. Запрещено использовать `mt_rand()`.
3. Запрещено использовать недетерминированные источники.
4. Все случайные вызовы должны иметь понятный `scope`.
5. Все случайные вызовы должны происходить в детерминированном порядке.
6. RNG должен синхронизироваться с тиком.
